<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.messengerApp">

    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">
        <activity
            android:name=".authenticationActivity.ResetPasswordActivity"
            android:exported="false">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".authenticationActivity.LogInActivity" />
        </activity>
        <activity
            android:name=".authenticationActivity.SignUpActivity"
            android:exported="false">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".authenticationActivity.LogInActivity" />
        </activity>
        <activity
            android:name=".authenticationActivity.LogInActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".chatActivity.ChatActivity"
            android:exported="false"
            android:label="@string/title_activity_item_list">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".listActivity.MainActivity" />
        </activity>
        <activity
            android:name=".listActivity.MainActivity"
            android:exported="true"/>
    </application>

</manifest>